import Card from '../Card';
import Exchange from '../Exchange';
import Level from '../Level';
import Location from '../Location';

type ApiData = Card | Element | Exchange | Level | Location;

export default ApiData;
