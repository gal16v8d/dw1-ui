import React from 'react';
import { useForm } from 'react-hook-form';
import CrudData from '../../api/model/crud/CrudData';

interface DigimonViewProps {
  crudInfo: CrudData;
}

const DigimonView: React.FC<DigimonViewProps> = ({ crudInfo }): JSX.Element => {
  return <></>;
};
